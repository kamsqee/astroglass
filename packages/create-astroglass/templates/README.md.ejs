<%# README.md — generated by create-astroglass CLI %>
# <%= projectName %>

Built with [Astroglass](https://github.com/kamsqee/astroglass-preview) — a premium Astro template with <%= themes.length %> theme<%= themes.length > 1 ? 's' : '' %>.

## 🚀 Quick Start

```bash
# Install dependencies
pnpm install

# Start development server
pnpm dev

# Build for production
pnpm build

# Preview production build
pnpm preview
```

## 🎨 Themes

<% themes.forEach(theme => { %>
- **<%= theme.id.charAt(0).toUpperCase() + theme.id.slice(1) %>** — `/<%= theme.id %>`
<% }) %>

## 🎭 Color Palettes

<% palettes.forEach(palette => { %>
- <%= palette %>
<% }) %>
<% if (locales.length > 1) { %>

## 🌐 Languages

<% locales.forEach(locale => { %>
- `<%= locale %>`
<% }) %>
<% } %>
<% if (features.includes('blog')) { %>

## 📝 Blog

Add new blog posts in `src/content/blog/`. Each post is an MDX file with frontmatter.
<% } %>
<% if (features.includes('docs')) { %>

## 📚 Documentation

Add documentation pages in `src/content/docs/`. Uses Fuse.js for full-text search.
<% } %>

## 📁 Project Structure

```
src/
├── components/     # UI components
├── config/         # Site configuration
├── content/        # Content collections (blog, docs)
├── layouts/        # Page layouts
├── pages/          # Route pages
├── styles/         # CSS and theme files
└── utils/          # Utility functions
```

## 🚢 Deployment

This project is configured for **<%= deployTarget %>** deployment.

```bash
pnpm build
```

---

Made with ♥ using [Astroglass](https://github.com/kamsqee/astroglass-preview)
